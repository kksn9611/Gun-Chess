using UnityEngine;
using System.Collections.Generic;
using Priority_Queue;

public class Pathfinder : MonoBehaviour
{
    public List<TileScript> FindPath(TileScript start, TileScript end)
    {

        return new List<TileScript>();
    }
}
